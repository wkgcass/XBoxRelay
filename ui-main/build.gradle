compileJava {
    options.compilerArgs << '--add-modules' << [
            'javafx.controls',
            'io.vproxy.vfx',
            'io.vertx.core',
            'kotlin.stdlib',
            'net.cassite.xboxrelay.base',
            'net.cassite.xboxrelay.ui',
    ].join(',')
}

jar {
    archiveName "xboxrelay-ui-main.jar"
}
